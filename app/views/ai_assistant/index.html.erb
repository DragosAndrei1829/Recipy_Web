<% content_for :title, "Chef AI - Asistent Culinar" %>

<div class="ai-assistant-container max-w-4xl mx-auto">
  <!-- Header -->
  <div class="ai-header bg-gradient-to-r from-emerald-600 to-teal-500 rounded-2xl p-6 mb-6 text-white shadow-xl">
    <div class="flex items-center justify-between">
      <div class="flex items-center gap-4">
        <div class="w-16 h-16 bg-white/20 rounded-2xl flex items-center justify-center backdrop-blur-sm">
          <span class="text-4xl">👨‍🍳</span>
        </div>
        <div>
          <h1 class="text-2xl font-bold">Chef AI</h1>
          <p class="text-white/80">Spune-mi ce ingrediente ai și te ajut să gătești ceva delicios!</p>
        </div>
      </div>
      
      <!-- AI Provider Selector -->
      <div class="hidden sm:block">
        <div class="bg-white/10 backdrop-blur-sm rounded-xl p-2">
          <label class="text-xs text-white/60 block mb-1 px-2">Motor AI:</label>
          <div class="flex gap-1" id="ai-provider-selector">
            <button type="button" 
                    class="provider-btn px-3 py-1.5 rounded-lg text-xs font-medium transition-all <%= @current_provider == 'local' ? 'bg-white text-emerald-600' : 'text-white/80 hover:bg-white/20' %>"
                    data-provider="local"
                    title="Căutare în rețete existente (gratuit)">
              🔍 Local
            </button>
            <button type="button" 
                    class="provider-btn px-3 py-1.5 rounded-lg text-xs font-medium transition-all <%= @current_provider == 'llama' ? 'bg-white text-emerald-600' : 'text-white/80 hover:bg-white/20' %>"
                    data-provider="llama"
                    title="Generare cu Llama 3.1 (gratuit)">
              🦙 Llama
            </button>
            <button type="button" 
                    class="provider-btn px-3 py-1.5 rounded-lg text-xs font-medium transition-all <%= @current_provider == 'openai' ? 'bg-white text-emerald-600' : 'text-white/80 hover:bg-white/20' %> <%= ENV['OPENAI_API_KEY'].blank? ? 'opacity-50 cursor-not-allowed' : '' %>"
                    data-provider="openai"
                    title="<%= ENV['OPENAI_API_KEY'].present? ? 'Generare cu GPT-4 (premium)' : 'Necesită OPENAI_API_KEY' %>"
                    <%= 'disabled' if ENV['OPENAI_API_KEY'].blank? %>>
              ✨ OpenAI
            </button>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Info badges -->
    <div class="mt-4 flex flex-wrap gap-2">
      <span class="px-3 py-1 bg-white/20 rounded-full text-sm backdrop-blur-sm">🆓 Căutare locală gratuită</span>
      <span class="px-3 py-1 bg-white/20 rounded-full text-sm backdrop-blur-sm">🦙 Llama 3.1 gratuit</span>
      <span class="px-3 py-1 bg-white/20 rounded-full text-sm backdrop-blur-sm">⏱️ Gătit rapid</span>
      <span class="px-3 py-1 bg-white/20 rounded-full text-sm backdrop-blur-sm">🌱 Vegetarian/Vegan</span>
    </div>
    
    <!-- Mobile Provider Selector -->
    <div class="sm:hidden mt-4">
      <select id="ai-provider-mobile" class="w-full px-3 py-2 bg-white/20 backdrop-blur-sm rounded-lg text-white text-sm border border-white/30">
        <option value="local" <%= 'selected' if @current_provider == 'local' %>>🔍 Local (gratuit)</option>
        <option value="llama" <%= 'selected' if @current_provider == 'llama' %>>🦙 Llama (gratuit)</option>
        <option value="openai" <%= 'selected' if @current_provider == 'openai' %> <%= 'disabled' if ENV['OPENAI_API_KEY'].blank? %>>✨ OpenAI (premium)</option>
      </select>
    </div>
  </div>

  <!-- How it works -->
  <div class="mb-6 p-4 bg-gradient-to-r from-blue-50 to-indigo-50 dark:from-blue-900/20 dark:to-indigo-900/20 rounded-xl border border-blue-100 dark:border-blue-800">
    <h3 class="font-semibold text-blue-800 dark:text-blue-300 mb-2">🔄 Cum funcționează:</h3>
    <ol class="text-sm text-blue-700 dark:text-blue-400 space-y-1">
      <li><strong>1. Local (gratuit):</strong> Căutăm în rețetele existente din comunitate</li>
      <li><strong>2. Llama (gratuit):</strong> Dacă nu găsim, generăm cu AI local (necesită Ollama)</li>
      <li><strong>3. OpenAI (opțional):</strong> Generare premium cu GPT-4 (necesită API key)</li>
    </ol>
  </div>

  <!-- Chat Container -->
  <div class="ai-chat-container bg-white dark:bg-gray-800 rounded-2xl shadow-xl overflow-hidden border border-gray-100 dark:border-gray-700">
    <!-- Messages Area -->
    <div id="ai-messages" class="ai-messages p-6 space-y-4 min-h-[400px] max-h-[60vh] overflow-y-auto">
      <% if @conversation_history.empty? %>
        <!-- Welcome Message -->
        <div class="ai-welcome text-center py-8">
          <div class="w-20 h-20 mx-auto mb-4 bg-gradient-to-br from-emerald-100 to-teal-100 dark:from-emerald-900/30 dark:to-teal-900/30 rounded-full flex items-center justify-center">
            <span class="text-4xl">👨‍🍳</span>
          </div>
          <h2 class="text-xl font-semibold text-gray-800 dark:text-white mb-2">Bună! Sunt Chef AI</h2>
          <p class="text-gray-600 dark:text-gray-400 max-w-md mx-auto">
            Spune-mi ce ingrediente ai în bucătărie și îți voi căuta rețete potrivite sau îți voi crea una nouă!
          </p>
          
          <!-- Provider Info -->
          <div class="mt-4 inline-flex items-center gap-2 px-4 py-2 bg-gray-100 dark:bg-gray-700 rounded-full text-sm">
            <span class="text-gray-500 dark:text-gray-400">Motor activ:</span>
            <span class="font-medium text-emerald-600 dark:text-emerald-400" id="current-provider-display">
              <%= case @current_provider
                  when 'local' then '🔍 Local (gratuit)'
                  when 'llama' then '🦙 Llama (gratuit)'
                  when 'openai' then '✨ OpenAI (premium)'
                  end %>
            </span>
          </div>
          
          <!-- Quick Suggestions -->
          <div class="mt-6 flex flex-wrap justify-center gap-2">
            <button type="button" class="ai-suggestion-btn px-4 py-2 bg-gray-100 dark:bg-gray-700 hover:bg-emerald-100 dark:hover:bg-emerald-900/30 rounded-xl text-sm text-gray-700 dark:text-gray-300 transition-colors" data-message="Am pui, roșii și usturoi. Ce pot găti?">
              🍗 Pui, roșii, usturoi
            </button>
            <button type="button" class="ai-suggestion-btn px-4 py-2 bg-gray-100 dark:bg-gray-700 hover:bg-emerald-100 dark:hover:bg-emerald-900/30 rounded-xl text-sm text-gray-700 dark:text-gray-300 transition-colors" data-message="Vreau ceva sănătos cu legume, la cuptor">
              🥗 Sănătos la cuptor
            </button>
            <button type="button" class="ai-suggestion-btn px-4 py-2 bg-gray-100 dark:bg-gray-700 hover:bg-emerald-100 dark:hover:bg-emerald-900/30 rounded-xl text-sm text-gray-700 dark:text-gray-300 transition-colors" data-message="Am paste, brânză și ouă. Ceva rapid în 20 de minute">
              🍝 Paste rapide
            </button>
            <button type="button" class="ai-suggestion-btn px-4 py-2 bg-gray-100 dark:bg-gray-700 hover:bg-emerald-100 dark:hover:bg-emerald-900/30 rounded-xl text-sm text-gray-700 dark:text-gray-300 transition-colors" data-message="Sunt vegetarian și am cartofi, ciuperci și smântână">
              🌱 Vegetarian
            </button>
          </div>
        </div>
      <% else %>
        <% @conversation_history.each do |msg| %>
          <% if msg[:role] == "user" %>
            <%= render "ai_assistant/user_message", message: msg[:content] %>
          <% else %>
            <%= render "ai_assistant/assistant_message", response: msg[:content] %>
          <% end %>
        <% end %>
      <% end %>
    </div>

    <!-- Input Area -->
    <div class="ai-input-area border-t border-gray-100 dark:border-gray-700 p-4 bg-gray-50 dark:bg-gray-800/50">
      <%= render "ai_assistant/input_form", current_provider: @current_provider %>
    </div>
  </div>

  <!-- Actions -->
  <div class="mt-4 flex justify-between items-center">
    <%= link_to clear_ai_conversation_path, method: :delete, class: "text-sm text-gray-500 hover:text-red-500 transition-colors", data: { turbo_method: :delete, turbo_confirm: "Ești sigur că vrei să ștergi conversația?" } do %>
      <svg class="w-4 h-4 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
      </svg>
      Șterge conversația
    <% end %>
    
    <p class="text-xs text-gray-400">
      🆓 Căutare locală gratuită • 🦙 Llama gratuit • ✨ OpenAI opțional
    </p>
  </div>
</div>

<style>
  .ai-messages::-webkit-scrollbar {
    width: 6px;
  }
  .ai-messages::-webkit-scrollbar-track {
    background: transparent;
  }
  .ai-messages::-webkit-scrollbar-thumb {
    background: #d1d5db;
    border-radius: 3px;
  }
  .ai-messages::-webkit-scrollbar-thumb:hover {
    background: #9ca3af;
  }
  
  .provider-btn.active {
    background: white;
    color: #059669;
  }
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  let currentProvider = '<%= @current_provider %>';
  
  // Handle provider selection (desktop)
  document.querySelectorAll('.provider-btn').forEach(btn => {
    btn.addEventListener('click', function() {
      if (this.disabled) return;
      
      const provider = this.dataset.provider;
      setProvider(provider);
      
      // Update UI
      document.querySelectorAll('.provider-btn').forEach(b => {
        b.classList.remove('bg-white', 'text-emerald-600');
        b.classList.add('text-white/80');
      });
      this.classList.add('bg-white', 'text-emerald-600');
      this.classList.remove('text-white/80');
    });
  });
  
  // Handle provider selection (mobile)
  const mobileSelect = document.getElementById('ai-provider-mobile');
  if (mobileSelect) {
    mobileSelect.addEventListener('change', function() {
      setProvider(this.value);
    });
  }
  
  function setProvider(provider) {
    currentProvider = provider;
    
    // Update hidden input in form
    const providerInput = document.getElementById('ai-provider-input');
    if (providerInput) {
      providerInput.value = provider;
    }
    
    // Update display
    const display = document.getElementById('current-provider-display');
    if (display) {
      const labels = {
        'local': '🔍 Local (gratuit)',
        'llama': '🦙 Llama (gratuit)',
        'openai': '✨ OpenAI (premium)'
      };
      display.textContent = labels[provider] || provider;
    }
    
    // Save preference via AJAX
    fetch('<%= set_ai_provider_path rescue "#" %>', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
      },
      body: JSON.stringify({ provider: provider })
    });
  }
  
  // Handle suggestion buttons
  document.querySelectorAll('.ai-suggestion-btn').forEach(btn => {
    btn.addEventListener('click', function() {
      const message = this.dataset.message;
      const input = document.getElementById('ai-message-input');
      if (input) {
        input.value = message;
        input.focus();
      }
    });
  });
  
  // Auto-scroll to bottom of messages
  const messagesContainer = document.getElementById('ai-messages');
  if (messagesContainer) {
    messagesContainer.scrollTop = messagesContainer.scrollHeight;
  }
  
  // Observe for new messages and scroll
  const observer = new MutationObserver(() => {
    messagesContainer.scrollTop = messagesContainer.scrollHeight;
  });
  
  if (messagesContainer) {
    observer.observe(messagesContainer, { childList: true, subtree: true });
  }
});
</script>
