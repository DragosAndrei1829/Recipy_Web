<% content_for :title, "Creează un grup nou" %>

<div class="max-w-2xl mx-auto px-4 py-8">
  <!-- Back Link -->
  <div class="mb-6">
    <%= link_to groups_path, class: "inline-flex items-center gap-2 text-gray-600 dark:text-gray-400 hover:text-emerald-600 transition-colors" do %>
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
      </svg>
      Înapoi la grupuri
    <% end %>
  </div>

  <div class="bg-white dark:bg-gray-800 rounded-2xl border border-gray-200 dark:border-gray-700 overflow-hidden shadow-xl">
    <!-- Header -->
    <div class="px-6 py-8 bg-gradient-to-r from-emerald-600 to-teal-500 text-white text-center">
      <div class="w-20 h-20 mx-auto mb-4 bg-white/20 rounded-2xl flex items-center justify-center backdrop-blur-sm">
        <span class="text-4xl">👥</span>
      </div>
      <h1 class="text-2xl font-bold">Creează un grup nou</h1>
      <p class="text-white/80 mt-2">Partajează rețete cu prietenii și familia într-un spațiu privat</p>
    </div>
    
    <!-- Form -->
    <div class="p-6">
      <%= form_with model: @group, local: true, class: "space-y-6" do |f| %>
        <% if @group.errors.any? %>
          <div class="p-4 bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-xl">
            <ul class="list-disc list-inside text-red-600 dark:text-red-400 text-sm">
              <% @group.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        
        <!-- Name -->
        <div>
          <%= f.label :name, "Nume grup", class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" %>
          <%= f.text_field :name, placeholder: "Ex: Rețete pentru slăbit", class: "w-full px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-800 dark:text-white focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500" %>
        </div>
        
        <!-- Description -->
        <div>
          <%= f.label :description, "Descriere (opțional)", class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" %>
          <%= f.text_area :description, rows: 3, placeholder: "Descrie scopul acestui grup...", class: "w-full px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-xl bg-white dark:bg-gray-700 text-gray-800 dark:text-white focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500 resize-none" %>
        </div>
        
        <!-- Cover Image -->
        <div>
          <%= f.label :cover_image, "Imagine de copertă (opțional)", class: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2" %>
          <div class="border-2 border-dashed border-gray-300 dark:border-gray-600 rounded-xl p-6 text-center hover:border-emerald-500 transition-colors">
            <%= f.file_field :cover_image, accept: "image/*", class: "hidden", id: "cover-image-input" %>
            <label for="cover-image-input" class="cursor-pointer">
              <svg class="w-12 h-12 mx-auto text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
              </svg>
              <p class="mt-2 text-sm text-gray-600 dark:text-gray-400">Click pentru a încărca o imagine</p>
            </label>
          </div>
        </div>
        
        <!-- Privacy -->
        <div class="flex items-center gap-3 p-4 bg-gray-50 dark:bg-gray-700/50 rounded-xl">
          <%= f.check_box :is_private, checked: true, class: "w-5 h-5 text-emerald-600 border-gray-300 rounded focus:ring-emerald-500" %>
          <div>
            <%= f.label :is_private, "Grup privat", class: "font-medium text-gray-800 dark:text-white" %>
            <p class="text-sm text-gray-600 dark:text-gray-400">Doar membrii invitați pot vedea conținutul</p>
          </div>
        </div>
        
        <!-- Info -->
        <div class="p-4 bg-blue-50 dark:bg-blue-900/20 rounded-xl">
          <p class="text-sm text-blue-700 dark:text-blue-300">
            <strong>💡 Notă:</strong> După creare, vei primi un cod de invitație pe care îl poți distribui persoanelor pe care vrei să le adaugi în grup.
          </p>
        </div>
        
        <!-- Submit -->
        <div class="flex gap-3">
          <%= link_to "Anulează", groups_path, class: "flex-1 px-4 py-3 bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 rounded-xl hover:bg-gray-200 dark:hover:bg-gray-600 transition-colors text-center" %>
          <%= f.submit "Creează grupul", class: "flex-1 px-4 py-3 bg-gradient-to-r from-emerald-600 to-teal-500 text-white rounded-xl hover:from-emerald-700 hover:to-teal-600 transition-all cursor-pointer font-medium" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

