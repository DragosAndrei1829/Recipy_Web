<!DOCTYPE html>
<html>
  <head>
    <title>Recipy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <% 
      # Get user's theme preference or default theme
      current_theme = if user_signed_in? && current_user.theme
        current_user.theme
      else
        SiteSetting.instance.current_theme
      end
    %>
    <% 
      site_setting = SiteSetting.instance
    %>
    <style>
      :root {
        --primary-color: <%= current_theme.primary_color %>;
        --secondary-color: <%= current_theme.secondary_color %>;
        --accent-color: <%= current_theme.accent_color %>;
        --navbar-color: <%= current_theme.navbar_color %>;
        --button-color: <%= current_theme.button_color %>;
        --link-color: <%= current_theme.link_color %>;
        --background-color: <%= current_theme.background_color %>;
        --card-background: <%= current_theme.card_background %>;
        --text-primary: <%= current_theme.text_primary %>;
        --text-secondary: <%= current_theme.text_secondary %>;
        --border-color: <%= current_theme.border_color %>;
        --success-color: <%= current_theme.success_color %>;
        --warning-color: <%= current_theme.warning_color %>;
        --error-color: <%= current_theme.error_color %>;
        --footer-background: <%= site_setting.footer_background || '#0f172a' %>;
        --footer-text: <%= site_setting.footer_text || '#ffffff' %>;
        --footer-link: <%= site_setting.footer_link || '#ffffff' %>;
        --footer-link-hover: <%= site_setting.footer_link_hover || '#a5b4fc' %>;
      }
      .bg-primary { background-color: var(--primary-color) !important; }
      .bg-secondary { background-color: var(--secondary-color) !important; }
      .bg-accent { background-color: var(--accent-color) !important; }
      .bg-navbar { background-color: var(--navbar-color) !important; }
      .bg-button { background-color: var(--button-color) !important; }
      .bg-card { background-color: var(--card-background) !important; }
      .bg-page { background-color: var(--background-color) !important; }
      .text-primary { color: var(--primary-color) !important; }
      .text-secondary { color: var(--secondary-color) !important; }
      .text-accent { color: var(--accent-color) !important; }
      .text-primary-content { color: var(--text-primary) !important; }
      .text-secondary-content { color: var(--text-secondary) !important; }
      .text-link { color: var(--link-color) !important; }
      .text-success { color: var(--success-color) !important; }
      .text-warning { color: var(--warning-color) !important; }
      .text-error { color: var(--error-color) !important; }
      .border-primary { border-color: var(--primary-color) !important; }
      .border-secondary { border-color: var(--secondary-color) !important; }
      .border-accent { border-color: var(--accent-color) !important; }
      .border-default { border-color: var(--border-color) !important; }
      .bg-gradient-primary { background: linear-gradient(to right, var(--primary-color), var(--secondary-color)) !important; }
      .bg-gradient-full { background: linear-gradient(to right, var(--primary-color), var(--secondary-color), var(--accent-color)) !important; }
      .bg-clip-text-primary { 
        background: linear-gradient(to right, var(--primary-color), var(--secondary-color), var(--accent-color)) !important; 
        -webkit-background-clip: text !important; 
        background-clip: text !important; 
      }
      .hover\:text-primary:hover { color: var(--primary-color) !important; }
      a { color: var(--link-color) !important; }
      a:hover { color: var(--primary-color) !important; }
      /* Force white text on buttons with primary/secondary/accent backgrounds */
      .bg-primary.text-white,
      .bg-secondary.text-white,
      .bg-accent.text-white,
      .bg-button.text-white,
      .bg-gradient-primary.text-white,
      .bg-gradient-full.text-white,
      button.bg-primary,
      button.bg-secondary,
      button.bg-accent,
      button.bg-button,
      button.bg-gradient-primary,
      button.bg-gradient-full,
      a.bg-primary,
      a.bg-secondary,
      a.bg-accent,
      a.bg-button,
      a.bg-gradient-primary,
      a.bg-gradient-full {
        color: #ffffff !important;
      }
    </style>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "animations", "data-turbo-track": "reload" %>
    <%= render 'layouts/theme_styles' %>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>

  <body class="theme-body text-primary-content <%= 'chat-fullpage' if defined?(@full_screen_chat) && @full_screen_chat %>" style="color: var(--text-primary);" data-theme="<%= current_theme.name.downcase %>">
    <!-- Animated background elements -->
    <div class="theme-background-pattern"></div>
    <% unless current_theme.name.downcase == 'dark' %>
      <div class="theme-particles">
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
        <div class="theme-particle"></div>
      </div>
    <% end %>
    <div class="app-shell space-y-6">
      <!-- Top Nav -->
      <nav class="app-nav glass-panel sticky top-4 z-40 animate-fade-in">
        <% if mobile_device? %>
          <!-- MOBILE NAVBAR - Modern Redesign -->
          <div class="flex h-14 items-center justify-between px-3 gap-2 bg-gradient-to-r from-card/95 via-card to-card/95 backdrop-blur-xl">
            <!-- Left: Logo only (clean) -->
            <div class="flex items-center gap-2">
              <%= link_to root_path, class: "select-none font-black text-lg flex items-center gap-2" do %>
                <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-emerald-500 via-teal-500 to-cyan-500 flex items-center justify-center shadow-lg">
                  <span class="text-white text-sm font-black">R</span>
                </div>
                <span class="bg-gradient-to-r from-emerald-500 to-teal-500 bg-clip-text text-transparent">Recipy</span>
              <% end %>
            </div>
            
            <!-- Right: Action Icons - Minimal -->
            <div class="flex items-center gap-1.5">
              <!-- Search Icon -->
              <button type="button" class="inline-flex items-center justify-center w-9 h-9 rounded-lg text-primary-content/70 hover:bg-primary/10 active:scale-95 transition-all" onclick="document.getElementById('mobile-search-modal').classList.remove('hidden')">
                <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
              </button>
              
              <% if user_signed_in? %>
                <!-- Add Recipe - Mobile FAB -->
                <%= link_to new_recipe_path(locale: I18n.locale), class: "inline-flex items-center justify-center w-9 h-9 rounded-lg text-primary-content/70 hover:bg-primary/10 active:scale-95 transition-all" do %>
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/>
                  </svg>
                <% end %>
                
                <!-- Notifications - Minimal -->
                <%= link_to notifications_path(locale: I18n.locale), class: "relative inline-flex items-center justify-center w-9 h-9 rounded-lg text-primary-content/70 hover:bg-primary/10 active:scale-95 transition-all" do %>
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
                  </svg>
                  <% if current_user.unread_notifications.count > 0 %>
                    <span class="absolute -top-0.5 -right-0.5 w-4 h-4 bg-gradient-to-r from-red-500 to-rose-500 text-white rounded-full text-[10px] font-bold flex items-center justify-center">
                      <%= current_user.unread_notifications.count > 9 ? '9' : current_user.unread_notifications.count %>
                    </span>
                  <% end %>
                <% end %>
                
                <!-- Menu Button (opens sidebar) -->
                <button id="menu-btn" class="inline-flex items-center justify-center w-9 h-9 rounded-lg text-primary-content/70 hover:bg-primary/10 active:scale-95 transition-all" aria-label="Open menu">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" d="M4 6h16M4 12h16M4 18h16"/></svg>
                </button>
              <% else %>
                <%= link_to new_user_session_path(locale: I18n.locale), class: "text-xs font-semibold text-primary-content px-3 py-1.5 rounded-lg bg-card border border-default" do %>
                  Login
                <% end %>
              <% end %>
            </div>
          </div>
          
          <!-- Mobile Search Modal - Enhanced -->
          <div id="mobile-search-modal" class="hidden fixed inset-0 bg-black/60 backdrop-blur-sm z-50 animate-fade-in" onclick="this.classList.add('hidden')">
            <div class="bg-card m-4 mt-24 rounded-3xl p-5 shadow-2xl border-2 border-primary/20 animate-slide-in" onclick="event.stopPropagation()">
              <!-- Close button -->
              <button type="button" class="absolute top-3 right-3 w-8 h-8 rounded-full bg-gray-100 dark:bg-gray-800 flex items-center justify-center" onclick="document.getElementById('mobile-search-modal').classList.add('hidden')">
                <svg class="w-5 h-5 text-gray-600 dark:text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
                </svg>
              </button>
              
              <div class="relative">
                <input type="text" placeholder="<%= t('navigation.search_all') %>" class="w-full pl-12 pr-4 py-4 text-base font-medium border-2 border-primary/30 rounded-2xl focus:outline-none focus:ring-4 focus:ring-primary/20 focus:border-primary bg-gradient-to-r from-white to-gray-50 dark:from-gray-800 dark:to-gray-900 shadow-inner transition-all" id="mobile-search-input" data-locale="<%= I18n.locale %>">
                <svg class="absolute left-4 top-1/2 -translate-y-1/2 h-6 w-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
              </div>
              <div id="mobile-search-results" class="mt-4 max-h-96 overflow-y-auto rounded-xl"></div>
            </div>
          </div>
        <% else %>
          <!-- DESKTOP NAVBAR - Full -->
          <div class="flex h-16 items-center gap-4">
            <!-- Left: Hamburger (mobile) + Brand + Admin Hub -->
            <div class="flex items-center gap-3 flex-shrink-0">
              <button id="menu-btn" class="inline-flex items-center justify-center rounded-xl p-2 text-primary-content/80 hover:bg-card transition" aria-label="Open menu" title="Deschide meniul">
                <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
              </button>
              <%= link_to root_path, class: "select-none font-black tracking-tight text-xl sm:text-2xl bg-clip-text-primary text-transparent hover:scale-105 transition-transform duration-300 flex-shrink-0" do %>
                Recipy
              <% end %>
              <% if user_signed_in? && current_user.admin? %>
                <%= link_to admin_dashboard_path, class: "inline-flex items-center gap-1.5 px-3 py-1.5 text-xs font-bold text-white bg-gradient-to-r from-red-600 to-orange-600 rounded-lg hover:from-red-700 hover:to-orange-700 shadow-md hover:shadow-lg transition-all duration-300" do %>
                  <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                  </svg>
                  <span>Admin Hub</span>
                <% end %>
              <% end %>
            </div>

            <!-- Center: Unified Search Bar - Enhanced Design -->
            <div class="flex-1 flex justify-center max-w-2xl mx-auto">
              <div class="relative w-full">
                <div class="relative group">
                  <input type="text" placeholder="<%= t('navigation.search_all') %>" 
                    class="w-full pl-14 pr-6 py-3.5 text-base font-medium border-2 border-primary/20 rounded-2xl focus:outline-none focus:ring-4 focus:ring-primary/20 focus:border-primary bg-gradient-to-r from-white/95 to-gray-50/95 dark:from-gray-800/95 dark:to-gray-900/95 backdrop-blur-md transition-all duration-300 shadow-lg hover:shadow-xl hover:border-primary/40 placeholder-gray-400 dark:placeholder-gray-500" 
                    id="unified-search-input" 
                    data-locale="<%= I18n.locale %>">
                  <div class="absolute left-4 top-1/2 -translate-y-1/2 w-8 h-8 rounded-lg bg-gradient-to-br from-emerald-400 to-teal-500 flex items-center justify-center shadow-md group-focus-within:scale-110 transition-transform">
                    <svg class="h-5 w-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                    </svg>
                  </div>
                </div>
                <div id="unified-search-results" class="absolute top-full left-0 right-0 mt-3 bg-card/95 backdrop-blur-xl rounded-2xl shadow-2xl border-2 border-primary/30 z-50 hidden max-h-96 overflow-hidden"></div>
              </div>
            </div>

            <!-- Right: Actions (Always Right Aligned) -->
            <div class="flex items-center gap-2 flex-shrink-0">
        <% end %>
              <% if user_signed_in? %>
                <!-- Notifications Button -->
                <%= link_to notifications_path(locale: I18n.locale), class: "relative inline-flex items-center justify-center w-10 h-10 rounded-full bg-card border-2 border-default hover:border-primary transition-all hover:shadow-md", title: t('notifications.title') do %>
                  <svg class="h-5 w-5 text-primary-content" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
                  </svg>
                  <% if current_user.unread_notifications.count > 0 %>
                    <span class="absolute -top-1 -right-1 px-1.5 py-0.5 text-xs font-bold bg-error text-white rounded-full min-w-[18px] text-center">
                      <%= current_user.unread_notifications.count > 9 ? '9+' : current_user.unread_notifications.count %>
                    </span>
                  <% end %>
                <% end %>

                <!-- Messages Button -->
                <%= link_to conversations_path(locale: I18n.locale), class: "relative inline-flex items-center justify-center w-10 h-10 rounded-full bg-card border-2 border-default hover:border-primary transition-all hover:shadow-md", title: t('conversations.title') do %>
                  <svg class="h-5 w-5 text-primary-content" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
                  </svg>
                  <% if current_user.unread_messages_count > 0 %>
                    <span class="absolute -top-1 -right-1 px-1.5 py-0.5 text-xs font-bold bg-error text-white rounded-full min-w-[18px] text-center">
                      <%= current_user.unread_messages_count > 9 ? '9+' : current_user.unread_messages_count %>
                    </span>
                  <% end %>
                <% end %>

                <!-- Add Recipe Button -->
                <%= link_to new_recipe_path, class: "hidden sm:inline-flex items-center gap-2 btn-primary px-4 py-2 text-sm" do %>
                  <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
                  </svg>
                  <span class="hidden lg:inline"><%= t('navigation.add_recipe') %></span>
                <% end %>
                
                <!-- Profile Dropdown -->
                <div class="relative">
                  <button type="button" class="inline-flex items-center focus:outline-none hover:ring-2 hover:ring-pink-300 rounded-full transition-all" id="profile-menu-button">
                    <% if current_user&.avatar&.attached? %>
                      <%= image_tag url_for(current_user.avatar.variant(resize_to_fill: [40,40])),
                          class: "h-10 w-10 rounded-full object-cover ring-2 ring-pink-100 bg-white cursor-pointer" %>
                    <% else %>
                      <div class="h-10 w-10 rounded-full bg-gradient-to-br from-pink-500 to-rose-400 ring-2 ring-pink-100 grid place-content-center text-white font-bold text-sm cursor-pointer">
                        <%= (current_user.username&.first || current_user.email&.first || 'U').upcase %>
                      </div>
                    <% end %>
                  </button>
                  
                  <!-- Simplified Profile Dropdown -->
                  <div id="profile-dropdown" class="hidden absolute right-0 mt-2 w-56 bg-card rounded-xl shadow-2xl border-2 border-default z-50 overflow-hidden">
                  <div class="py-1">
                    <%= link_to user_path(current_user, locale: I18n.locale), class: "block px-4 py-2.5 text-sm text-primary-content hover:bg-primary/10 transition-all" do %>
                      <div class="flex items-center gap-2">
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                        </svg>
                        <span><%= t('users.view_profile') %></span>
                      </div>
                    <% end %>
                    <%= link_to edit_user_registration_path(locale: I18n.locale), class: "block px-4 py-2.5 text-sm text-primary-content hover:bg-primary/10 transition-all" do %>
                      <div class="flex items-center gap-2">
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/>
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                        </svg>
                        <span><%= t('navigation.edit_profile') %></span>
                      </div>
                    <% end %>
                    <%= link_to purchases_user_path(current_user, locale: I18n.locale), class: "block px-4 py-2.5 text-sm text-primary-content hover:bg-primary/10 transition-all" do %>
                      <div class="flex items-center gap-2">
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
                        </svg>
                        <span><%= t('navigation.my_purchases', default: 'My Purchases') %></span>
                      </div>
                    <% end %>
                    <div class="px-4 py-2.5">
                      <label class="block text-sm text-primary-content mb-2 font-semibold"><%= t('navigation.select_theme') %></label>
                      <%= form_with url: change_theme_path(locale: I18n.locale), method: :post, local: true, data: { turbo: false }, id: "theme-select-form", class: "inline-block w-full" do |f| %>
                        <%= f.select :theme_id, 
                            options_from_collection_for_select(Theme.all, :id, :name, user_signed_in? ? current_user.theme_id : Theme.default&.id),
                            {},
                            { 
                              class: "w-full px-3 py-2 text-sm border-2 border-default rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary bg-card text-primary-content",
                              onchange: "this.form.submit();",
                              id: "theme-select"
                            } %>
                      <% end %>
                    </div>
                    <div class="border-t border-default my-1"></div>
                    <%= button_to destroy_user_session_path(locale: I18n.locale), method: :delete, form: { "data-turbo": false }, class: "w-full text-left block px-4 py-2.5 text-sm text-error hover:bg-error/10 transition-all" do %>
                      <div class="flex items-center gap-2">
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                        </svg>
                        <span><%= t('navigation.logout') %></span>
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>

              <!-- Language Switch Buttons (Always Visible) -->
              <div class="flex items-center gap-1 border-l border-gray-200 pl-2 ml-1">
                <% current_locale = I18n.locale.to_s %>
                <%= link_to switch_locale_path('ro'), 
                    class: "inline-flex items-center justify-center w-9 h-9 rounded-lg text-xs font-bold transition-all duration-300 #{current_locale == 'ro' ? 'bg-gradient-primary text-white shadow-md' : 'text-gray-600 hover:bg-gray-100 hover:text-primary'}",
                    title: t('language.ro') do %>
                  RO
                <% end %>
                <%= link_to switch_locale_path('en'), 
                    class: "inline-flex items-center justify-center w-9 h-9 rounded-lg text-xs font-bold transition-all duration-300 #{current_locale == 'en' ? 'bg-gradient-primary text-white shadow-md' : 'text-gray-600 hover:bg-gray-100 hover:text-primary'}",
                    title: t('language.en') do %>
                  EN
                <% end %>
              </div>

            <% else %>
              <!-- Not Signed In: Login Button + Language -->
              <%= link_to t('navigation.login'), new_user_session_path, class: "inline-flex items-center gap-2 btn-primary px-4 py-2 text-sm" %>
              
              <!-- Language Switch Buttons (Always Visible) -->
              <div class="flex items-center gap-1 border-l border-gray-200 pl-2 ml-1">
                <% current_locale = I18n.locale.to_s %>
                <%= link_to switch_locale_path('ro'), 
                    class: "inline-flex items-center justify-center w-9 h-9 rounded-lg text-xs font-bold transition-all duration-300 #{current_locale == 'ro' ? 'bg-gradient-primary text-white shadow-md' : 'text-gray-600 hover:bg-gray-100 hover:text-primary'}",
                    title: t('language.ro') do %>
                  RO
                <% end %>
                <%= link_to switch_locale_path('en'), 
                    class: "inline-flex items-center justify-center w-9 h-9 rounded-lg text-xs font-bold transition-all duration-300 #{current_locale == 'en' ? 'bg-gradient-primary text-white shadow-md' : 'text-gray-600 hover:bg-gray-100 hover:text-primary'}",
                    title: t('language.en') do %>
                  EN
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
        <div class="nav-quick-links hidden lg:flex items-center gap-3">
          <% if user_signed_in? && current_user.user_shortcuts.any? %>
            <% current_user.user_shortcuts.ordered.each do |shortcut| %>
              <%= link_to shortcut.url, class: "nav-pill", style: "--shortcut-color: #{shortcut.color};" do %>
                <span class="indicator" style="background-color: <%= shortcut.color %>;"></span>
                <span><%= shortcut.icon %> <%= shortcut.name %></span>
              <% end %>
            <% end %>
            <%= link_to user_shortcuts_path(locale: I18n.locale), class: "nav-pill-edit", title: t('user_shortcuts.manage') do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
              </svg>
              <span class="hidden xl:inline"><%= t('user_shortcuts.manage') %></span>
            <% end %>
          <% else %>
            <%= link_to recipes_path(locale: I18n.locale), class: "nav-pill" do %>
              <span class="indicator bg-primary/80"></span>
              <span><%= t('navigation.trending', default: 'Trending now') %></span>
            <% end %>
            <% if user_signed_in? %>
              <%= link_to favorites_path(locale: I18n.locale), class: "nav-pill" do %>
                <span class="indicator bg-rose-500"></span>
                <span><%= t('favorites.title') %></span>
              <% end %>
              <%= link_to new_recipe_path(locale: I18n.locale), class: "nav-pill nav-pill-accent" do %>
                <span class="indicator bg-emerald-500"></span>
                <span><%= t('navigation.add_recipe') %></span>
              <% end %>
              <%= link_to user_shortcuts_path(locale: I18n.locale), class: "nav-pill-edit", title: t('user_shortcuts.manage') do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                </svg>
                <span class="hidden xl:inline"><%= t('user_shortcuts.manage') %></span>
              <% end %>
            <% else %>
              <%= link_to new_user_session_path(locale: I18n.locale), class: "nav-pill" do %>
                <span class="indicator bg-amber-500"></span>
                <span><%= t('devise.sessions.title') %></span>
              <% end %>
            <% end %>
          <% end %>
        </div>
    </nav>

      <!-- Modern Sidebar Menu -->
      <%= render 'shared/modern_sidebar' %>

      <!-- Page Container with sidebar and feed -->
      <main class="app-main animate-fade-in <%= 'chat-fullpage-main' if defined?(@full_screen_chat) && @full_screen_chat %>" data-turbo-action="replace">
        <% if defined?(@full_screen_chat) && @full_screen_chat %>
          <section class="chat-fullpage-wrapper">
            <%= yield %>
          </section>
        <% else %>
          <% if controller_name == 'recipes' && action_name == 'top_recipes' %>
            <!-- Full Width Layout for Top Recipes -->
            <div class="top-recipes-page-container">
              <%= yield %>
            </div>
          <% elsif controller_name == 'recipes' && action_name == 'show' %>
            <!-- Full Width Layout for Recipe Show -->
            <div class="recipe-show-page-container">
              <%= yield %>
            </div>
          <% elsif controller_name == 'groups' && action_name == 'show' %>
            <!-- Full Width Layout for Groups -->
            <div class="group-view-page-container">
              <%= yield %>
            </div>
          <% else %>
            <div class="<%= mobile_device? ? 'mobile-layout' : 'app-grid' %>">
              <!-- Left Sidebar (hidden on mobile) -->
              <aside class="<%= mobile_device? ? 'hidden' : 'hidden lg:block' %>">
                <div class="sidebar-stack">
                  <% if controller_name == 'users' && action_name == 'show' && defined?(@user) %>
                    <%= render 'users/profile_sidebar', user: @user %>
                  <% elsif defined?(@top_day) %>
                    <div class="sidebar-card">
                      <%= render 'recipes/top_recipes_sidebar' %>
                    </div>
                    <div class="sidebar-card">
                      <%= render 'recipes/sidebar_insights' %>
                    </div>
                  <% end %>
                </div>
              </aside>
              
              <!-- Main Content -->
              <section class="feed-column">
                <%= yield %>
              </section>
              
              <!-- Right Sidebar (hidden on mobile) -->
              <aside class="<%= mobile_device? ? 'hidden' : 'hidden lg:block' %>">
                <div class="sidebar-stack">
                  <% if controller_name == 'users' && action_name == 'show' && defined?(@user) %>
                    <%= render 'users/stats_sidebar', user: @user %>
                  <% elsif controller_name == 'recipes' && action_name == 'index' %>
                    <%= render 'recipes/filters_sidebar' %>
                    <div class="sidebar-card">
                      <%= render 'recipes/sidebar_quick_filters' %>
                    </div>
                  <% end %>
                </div>
              </aside>
            </div>
          <% end %>
        <% end %>
      </main>

      <!-- Footer -->
      <%= render 'layouts/footer' %>
    </div>

    <%= render 'shared/cookie_banner' %>
    <%= render 'reports/report_modal' if user_signed_in? %>
    <%= render 'collections/add_to_collection_modal' if user_signed_in? %>

    <!-- Offcanvas logic and other initializations -->
    <script>
      // Initialize all page functionality
      function initializePage() {
        const currentLocale = document.documentElement.lang || '<%= I18n.locale %>';
        // Modern Sidebar Menu
        const menuBtn = document.getElementById('menu-btn');
        const closeBtn = document.getElementById('close-sidebar');
        const modernSidebar = document.getElementById('modern-sidebar');
        const sidebarOverlay = document.getElementById('sidebar-overlay');
        
        function openModernSidebar() {
          if (modernSidebar) {
            modernSidebar.classList.remove('-translate-x-full');
            if (sidebarOverlay) sidebarOverlay.classList.remove('hidden');
            document.body.style.overflow = 'hidden';
          }
        }
        
        function closeModernSidebar() {
          if (modernSidebar) {
            modernSidebar.classList.add('-translate-x-full');
            if (sidebarOverlay) sidebarOverlay.classList.add('hidden');
            document.body.style.overflow = '';
          }
        }
        
        if (menuBtn) {
          menuBtn.replaceWith(menuBtn.cloneNode(true));
          document.getElementById('menu-btn')?.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            openModernSidebar();
          });
        }
        
        if (closeBtn) {
          closeBtn.replaceWith(closeBtn.cloneNode(true));
          document.getElementById('close-sidebar')?.addEventListener('click', closeModernSidebar);
        }
        
        if (sidebarOverlay) {
          sidebarOverlay.addEventListener('click', closeModernSidebar);
        }
        
        // Close on escape key
        document.addEventListener('keydown', function(e) {
          if (e.key === 'Escape' && modernSidebar && !modernSidebar.classList.contains('-translate-x-full')) {
            closeModernSidebar();
          }
        });

        // Profile dropdown - reinitialize
        const profileButton = document.getElementById('profile-menu-button');
        const profileDropdown = document.getElementById('profile-dropdown');
        
        if (profileButton && profileDropdown) {
          // Remove old listeners by cloning
          const newButton = profileButton.cloneNode(true);
          profileButton.parentNode.replaceChild(newButton, profileButton);
          const newDropdown = document.getElementById('profile-dropdown');
          
          // Add fresh listeners
          const freshButton = document.getElementById('profile-menu-button');
          if (freshButton && newDropdown) {
            freshButton.addEventListener('click', (e) => {
              e.stopPropagation();
              e.preventDefault();
              newDropdown.classList.toggle('hidden');
            });
          }
        }
        
        // Close dropdown when clicking outside (single global listener)
        if (!window.profileDropdownHandler) {
          window.profileDropdownHandler = function(e) {
            const profileButton = document.getElementById('profile-menu-button');
            const profileDropdown = document.getElementById('profile-dropdown');
            if (profileButton && profileDropdown && 
                !profileButton.contains(e.target) && 
                !profileDropdown.contains(e.target)) {
              profileDropdown.classList.add('hidden');
            }
          };
          document.addEventListener('click', window.profileDropdownHandler);
        }


      // Unified search - reinitialize (Desktop + Mobile)
      const unifiedSearchInput = document.getElementById('unified-search-input');
      const unifiedSearchResults = document.getElementById('unified-search-results');
      const mobileSearchInput = document.getElementById('mobile-search-input');
      const mobileSearchResults = document.getElementById('mobile-search-results');
      let unifiedSearchTimeout;
      
      if (unifiedSearchInput && unifiedSearchResults) {
        // Remove old listener
        const newInput = unifiedSearchInput.cloneNode(true);
        unifiedSearchInput.parentNode.replaceChild(newInput, unifiedSearchInput);
        const freshInput = document.getElementById('unified-search-input');
        
        if (freshInput) {
          const quickMap = JSON.parse(freshInput.dataset.quickMap || '{}');
          const localeFromInput = freshInput.dataset.locale || currentLocale;

          const quickSlugFromValue = (value) => {
            if (!value) return null;
            const match = value.toLowerCase().match(/#([\w\-]+)/);
            if (match && quickMap[match[1]]) {
              return match[1];
            }
            return null;
          };

          freshInput.addEventListener('input', (e) => {
            clearTimeout(unifiedSearchTimeout);
            const query = e.target.value.trim();
            
            if (query.length < 2) {
              unifiedSearchResults.classList.add('hidden');
              return;
            }
            
            unifiedSearchTimeout = setTimeout(() => {
              const locale = localeFromInput || window.location.pathname.split('/')[1] || 'en';
              fetch(`/${locale}/search?q=${encodeURIComponent(query)}`, {
                headers: {
                  'Accept': 'text/html',
                  'X-Requested-With': 'XMLHttpRequest'
                }
              })
              .then(response => response.text())
              .then(html => {
                unifiedSearchResults.innerHTML = html;
                unifiedSearchResults.classList.remove('hidden');
              })
              .catch(() => {
                unifiedSearchResults.classList.add('hidden');
              });
            }, 300);
          });

          // Handle Enter key to navigate to search results page
          freshInput.addEventListener('keydown', (e) => {
            const value = freshInput.value.trim();
            const quickSlug = quickSlugFromValue(value);
            if (e.key === 'Enter') {
              if (quickSlug) {
                window.location.href = `/${localeFromInput}/recipes?preset=${quickSlug}`;
                return;
              }
              if (value.length >= 2) {
                window.location.href = `/${localeFromInput}/recipes?search=${encodeURIComponent(value)}`;
              }
            }
          });

          // Hide results when clicking outside (single global listener)
          if (!window.unifiedSearchResultsHandler) {
            window.unifiedSearchResultsHandler = function(e) {
              const searchInput = document.getElementById('unified-search-input');
              const searchResults = document.getElementById('unified-search-results');
              if (searchInput && searchResults &&
                  !searchInput.contains(e.target) && 
                  !searchResults.contains(e.target)) {
                searchResults.classList.add('hidden');
              }
            };
            document.addEventListener('click', window.unifiedSearchResultsHandler);
          }
        }
      }
      
      // Mobile search functionality
      if (mobileSearchInput && mobileSearchResults) {
        const newMobileInput = mobileSearchInput.cloneNode(true);
        mobileSearchInput.parentNode.replaceChild(newMobileInput, mobileSearchInput);
        const freshMobileInput = document.getElementById('mobile-search-input');
        
        if (freshMobileInput) {
          const localeFromInput = freshMobileInput.dataset.locale || currentLocale;
          
          freshMobileInput.addEventListener('input', (e) => {
            clearTimeout(unifiedSearchTimeout);
            const query = e.target.value.trim();
            
            if (query.length < 2) {
              mobileSearchResults.innerHTML = '';
              return;
            }
            
            unifiedSearchTimeout = setTimeout(() => {
              const locale = localeFromInput || window.location.pathname.split('/')[1] || 'en';
              fetch(`/${locale}/search?q=${encodeURIComponent(query)}`, {
                headers: {
                  'Accept': 'text/html',
                  'X-Requested-With': 'XMLHttpRequest'
                }
              })
              .then(response => response.text())
              .then(html => {
                mobileSearchResults.innerHTML = html;
              })
              .catch(() => {
                mobileSearchResults.innerHTML = '<p class="text-sm text-secondary-content p-4">Error loading results</p>';
              });
            }, 300);
          });
          
          freshMobileInput.addEventListener('keydown', (e) => {
            const value = freshMobileInput.value.trim();
            if (e.key === 'Enter' && value.length >= 2) {
              window.location.href = `/${localeFromInput}/recipes?search=${encodeURIComponent(value)}`;
            }
          });
        }
      }

      document.querySelectorAll('[data-quick-search]').forEach(button => {
        button.addEventListener('click', () => {
          const slug = button.dataset.quickSearch;
          if (slug) {
            window.location.href = `/${currentLocale}/recipes?preset=${slug}`;
          }
        });
      });

      document.querySelectorAll('[data-filter-toggle]').forEach(toggle => {
        const card = toggle.closest('.filters-card');
        const advancedSection = card?.querySelector('[data-filter-advanced]');
        if (!advancedSection) return;
        toggle.addEventListener('click', () => {
          advancedSection.classList.toggle('hidden');
          const expanded = toggle.getAttribute('aria-expanded') === 'true';
          toggle.setAttribute('aria-expanded', (!expanded).toString());
        });
      });

        // Stagger animation for recipe cards
        const staggerItems = document.querySelectorAll('.stagger-item');
        staggerItems.forEach((item, index) => {
          item.style.animationDelay = `${index * 0.1}s`;
        });
      }

      // Like button animation (global, doesn't need reinitialization)
      document.addEventListener('submit', (e) => {
        const form = e.target.closest('.like-button');
        if (form) {
          const button = form.querySelector('button');
          if (button) {
            button.classList.add('animate-like-pop');
            setTimeout(() => {
              button.classList.remove('animate-like-pop');
            }, 400);
          }
        }
      });

      // Page transition animation
      document.addEventListener('turbo:before-visit', () => {
        document.body.style.opacity = '0.7';
      });

      // Video timestamps functionality
      function initializeVideoTimestamps() {
        const videoPlayer = document.getElementById('recipe-video-player');
        if (!videoPlayer) return;

        const timestampsData = videoPlayer.dataset.timestamps;
        if (!timestampsData) return;

        const timestamps = JSON.parse(timestampsData);
        const timestampButtons = document.querySelectorAll('.timestamp-step');

        timestampButtons.forEach(button => {
          button.addEventListener('click', function() {
            const timestampSeconds = parseInt(this.dataset.timestamp);
            if (videoPlayer && !isNaN(timestampSeconds)) {
              videoPlayer.currentTime = timestampSeconds;
              videoPlayer.play();
              // Highlight active step
              timestampButtons.forEach(btn => btn.classList.remove('border-primary', 'bg-primary/10'));
              this.classList.add('border-primary', 'bg-primary/10');
            }
          });
        });

        // Update active step as video plays
        videoPlayer.addEventListener('timeupdate', function() {
          const currentTime = Math.floor(this.currentTime);
          timestampButtons.forEach(button => {
            const timestampSeconds = parseInt(button.dataset.timestamp);
            const nextTimestamp = Array.from(timestampButtons)
              .map(btn => parseInt(btn.dataset.timestamp))
              .filter(ts => ts > timestampSeconds)
              .sort((a, b) => a - b)[0] || videoPlayer.duration;

            if (currentTime >= timestampSeconds && currentTime < nextTimestamp) {
              timestampButtons.forEach(btn => btn.classList.remove('border-primary', 'bg-primary/10'));
              button.classList.add('border-primary', 'bg-primary/10');
            }
          });
        });
      }

      // Initialize on both DOMContentLoaded and turbo:load
      document.addEventListener('DOMContentLoaded', function() {
        initializePage();
        initializeVideoTimestamps();
      });
      document.addEventListener('turbo:load', () => {
        document.body.style.opacity = '1';
        // Animate in main content
        const main = document.querySelector('main');
        if (main) {
          main.classList.add('animate-fade-in');
        }
        // Reinitialize all functionality
        initializePage();
        initializeVideoTimestamps();
      });
    </script>
  </body>
</html>
