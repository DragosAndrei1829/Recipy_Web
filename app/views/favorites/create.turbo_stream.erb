<%= turbo_stream.replace "recipe_#{@recipe.id}_favorite_button" do %>
  <%= button_to recipe_favorite_path(@recipe, locale: I18n.locale), method: :delete, id: "recipe_#{@recipe.id}_favorite_button", class: "recipe-card-modern__action-btn recipe-card-modern__action-btn--favorite recipe-card-modern__action-btn--favorited action-btn-compact action-btn-compact--favorited" do %>
    <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
      <path d="M5 4a2 2 0 012-2h6a2 2 0 012 2v14l-5-2.5L5 18V4z"/>
    </svg>
  <% end %>
<% end %>
