<%
  # Safely get cookie preferences - handles edge cases during error rendering
  initial_prefs = nil
  begin
    initial_prefs = cookies[:cookie_preferences] if defined?(cookies) && cookies.respond_to?(:signed)
  rescue => e
    # Silently fail - cookie banner will show for new users anyway
  end
%>
<div data-controller="cookie-consent" data-cookie-consent-preferences-value="<%= initial_prefs %>">
  <!-- Banner -->
  <div data-cookie-consent-target="banner" class="fixed bottom-4 inset-x-4 md:inset-x-auto md:right-6 md:left-auto md:w-[420px] z-50 bg-white/95 dark:bg-gray-900/95 border-2 border-emerald-100 shadow-2xl rounded-2xl p-5 space-y-4 backdrop-blur hidden">
    <div class="flex items-start gap-3">
      <div class="flex-shrink-0 h-10 w-10 rounded-full bg-emerald-50 text-emerald-600 grid place-content-center">
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 3A7.5 7.5 0 002.25 10.5c0 3.638 2.57 6.677 6 7.36V21a.75.75 0 001.2.6l2.55-2.04a1.5 1.5 0 01.93-.31h.37c4.139 0 7.5-3.361 7.5-7.5S17.889 3 13.75 3h-4z" />
        </svg>
      </div>
      <div>
        <h3 class="text-lg font-semibold text-gray-900 dark:text-white">Preferințe cookie</h3>
        <p class="text-sm text-gray-600 dark:text-gray-300 leading-relaxed">
          Folosim cookie-uri esențiale pentru a opera platforma și opționale pentru personalizare, analize și marketing.
          Puteți accepta toate cookie-urile sau vă puteți ajusta preferințele.
        </p>
      </div>
    </div>
    <div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
      <button type="button" class="w-full sm:w-auto inline-flex justify-center rounded-xl bg-gradient-to-r from-emerald-500 to-teal-500 px-4 py-2 text-sm font-semibold text-white shadow-lg hover:shadow-xl transition" data-action="cookie-consent#acceptAll">
        Acceptă toate
      </button>
      <div class="flex flex-col sm:flex-row gap-2 w-full sm:w-auto">
        <button type="button" class="w-full inline-flex justify-center rounded-xl border border-emerald-200 px-4 py-2 text-sm font-semibold text-emerald-700 hover:bg-emerald-50 transition" data-action="cookie-consent#openPreferences">
          Personalizează
        </button>
        <button type="button" class="w-full inline-flex justify-center rounded-xl px-4 py-2 text-sm font-semibold text-gray-600 hover:bg-gray-100 transition" data-action="cookie-consent#essentialOnly">
          Doar esențiale
        </button>
      </div>
    </div>
  </div>

  <!-- Preferences Dialog -->
  <div data-cookie-consent-target="dialog" class="fixed inset-0 z-50 hidden">
    <div class="absolute inset-0 bg-black/40 backdrop-blur-sm" data-action="click->cookie-consent#closePreferences"></div>
    <div class="relative mx-auto mt-16 max-w-xl rounded-3xl bg-white dark:bg-gray-900 shadow-2xl border border-gray-100 dark:border-gray-800 p-6 space-y-6">
      <div class="flex items-start justify-between gap-4">
        <div>
          <p class="text-xs uppercase tracking-[0.3em] text-emerald-500 font-semibold">Preferințe cookie</p>
          <h3 class="text-3xl font-black text-gray-900 dark:text-white mt-1">Gestionează consimțământul</h3>
          <p class="text-sm text-gray-600 dark:text-gray-300 mt-2">Alege ce tipuri de cookie-uri acceptați. Puteți modifica opțiunile oricând din Politica de Cookie-uri.</p>
        </div>
        <button type="button" class="text-gray-500 hover:text-gray-800" aria-label="Închide" data-action="cookie-consent#closePreferences">
          <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>

      <div class="space-y-4">
        <div class="rounded-2xl border border-gray-200 dark:border-gray-800 p-4 bg-gray-50 dark:bg-gray-800/50">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-semibold text-gray-900 dark:text-white">Esențiale</p>
              <p class="text-xs text-gray-600 dark:text-gray-300">Necesare pentru funcționarea platformei (login, securitate). Active permanent.</p>
            </div>
            <input type="checkbox" checked disabled class="h-4 w-4 accent-emerald-500 cursor-not-allowed opacity-60" />
          </div>
        </div>

        <% [
          { key: :preferences, title: "Preferințe", desc: "Memorează limbă, temă și filtre pentru a personaliza experiența." },
          { key: :analytics, title: "Analiză & performanță", desc: "Ne ajută să înțelegem cum este folosită aplicația (ex. Google Analytics)." },
          { key: :marketing, title: "Marketing", desc: "Permite conținut personalizat și notificări comerciale relevante." }
        ].each do |group| %>
          <div class="rounded-2xl border border-gray-200 dark:border-gray-800 p-4">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-sm font-semibold text-gray-900 dark:text-white"><%= group[:title] %></p>
                <p class="text-xs text-gray-600 dark:text-gray-300"><%= group[:desc] %></p>
              </div>
              <label class="inline-flex items-center cursor-pointer gap-2">
                <span class="text-xs text-gray-500">Off</span>
                <input type="checkbox" class="sr-only" data-cookie-consent-target="prefCheckbox" data-category="<%= group[:key] %>">
                <span class="relative inline-flex h-6 w-11 items-center rounded-full border border-gray-200 dark:border-gray-700 bg-gray-200 dark:bg-gray-700 transition peer-checked:bg-emerald-500">
                  <span class="absolute left-0.5 h-5 w-5 rounded-full bg-white shadow transform transition peer-checked:translate-x-5"></span>
                </span>
                <span class="text-xs text-gray-500">On</span>
              </label>
            </div>
          </div>
        <% end %>
      </div>

      <div class="flex flex-col sm:flex-row sm:items-center sm:justify-end gap-3">
        <button type="button" class="w-full sm:w-auto inline-flex justify-center rounded-xl border border-gray-200 px-4 py-2 text-sm font-semibold text-gray-600 hover:bg-gray-100 transition" data-action="cookie-consent#essentialOnly">
          Numai esențiale
        </button>
        <button type="button" class="w-full sm:w-auto inline-flex justify-center rounded-xl bg-gradient-to-r from-emerald-500 to-teal-500 px-4 py-2 text-sm font-semibold text-white shadow-lg hover:shadow-xl transition" data-action="cookie-consent#saveCustom">
          Salvează preferințele
        </button>
      </div>
    </div>
  </div>
</div>

